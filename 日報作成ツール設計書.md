## 日報作成ツール設計書


## 1. 概要
本設計書は、日報作成ツールの設計に関する情報をまとめたものである。<br>
本ツールは、ユーザーが日々の業務内容を簡単に記録し、管理することを目的としている。

## 2. システム要件
### 2.1 機能要件
- 日報には、日付、定例作業内容、スキルアップ活動と、成果物を入力できる。
- ユーザーは入力した内容をmarkdown形式でエクスポートできる。
- ユーザー名を設定・変更でき、次回起動時にもその設定を引き継ぐ。

### 2.2 技術要件
- 開発言語: C#
- 出力の種類：Windowsアプリケーション
- フレームワーク: .NET Framework4.8
- データベース: なし
- UIフレームワーク: WPF
- デザインパターン: MVVM
- データ保存形式: markdown形式
- 文字コード: UTF-8（BOMなし）

## 3. システム構成
### 3.1 画面構成
- メイン画面のみで構成する。
  - 名前の設定・変更欄
  - 日付の選択欄
  - 定例作業、スキルアップ活動、成果物の行追加・行削除ボタン
  - 保存ボタン

### 3.2 入力欄
- 名前： TextBoxコントロールを使用する。アプリケーション設定に保存する。
- 日付： DatePickerコントロールを使用（直接入力せず、選択形式とする）。
- 定例作業： リスト形式で入力するためのDataGridコントロールを使用（タイトル：定例作業）
- スキルアップ活動： リスト形式で入力するためのDataGridコントロールを使用(タイトル：スキルアップ活動)
- 成果物: 2列のリスト形式で入力するためのDataGridコントロールを使用(タイトル：成果物、列1：成果物名、列2：リンク)

各DataGridは、空白を除く内容が入力された行だけを日報データおよび出力対象とする。利用者は各リストに行を追加・削除できる。

### 3.3 日報データ構造
- 日報データは、以下のクラスで表現する。
```csharp
public class DailyReport
{
  public string Name { get; set; }
  public DateTime Date { get; set; }
  public ObservableCollection<TaskItem> RegularTasks { get; set; }
  public ObservableCollection<TaskItem> SkillUpActivities { get; set; }
  public ObservableCollection<Deliverable> Deliverables { get; set; }
}

public class TaskItem
{
  public string Content { get; set; }
}

public class Deliverable
{
    public string Name { get; set; }
    public string Link { get; set; }
}
```

- `RegularTasks`および`SkillUpActivities`のDataGridは、`TaskItem.Content`を1列で編集する。
- `Deliverables`のDataGridは、`Deliverable.Name`と`Deliverable.Link`を編集する。
- ViewModelは各コレクションと、行追加・行削除・保存のコマンドを公開する。ファイル出力は保存サービスが担当し、Viewはデータバインディングと操作イベントの表示だけを担当する。

### 3.4 機能概要
- 名前はアプリケーション設定で保持する。起動時に設定値を読み込んで日報データに設定し、変更時は設定を更新する。
- 起動時に、当日の日付を日付入力欄に表示する。
- 定例作業のリストには、先頭に以下を表示しておく。
  - 朝礼／夕礼
- スキルアップ活動のリストは、起動時に空行で初期化する。
- 成果物のリストは、起動時に空行で初期化する。
- 保存成功後は、保存した入力内容を画面に維持し、利用者が内容を確認または別名で再保存できるようにする。

### 3.5 データ保存時のエラー検証
- 名前が空または空白のみの場合はエラーとする。
- 日付が未指定の場合はエラーとする。
- 定例作業とスキルアップ活動の両方に、空白を除く入力済み行が存在しない場合はエラーとする。
- 成果物は、全行が空の場合はエラーとしない。成果物名が未入力でリンクのみ入力されている場合はエラーとする。成果物名のみの入力は許可する。
- リンクを入力する場合は、`http`または`https`の絶対URLでなければエラーとする。
- 検証エラー時は対象項目とエラー内容を画面に表示し、入力内容は維持する。

### 3.6 データ保存
上記のエラーチェックが完了した後、以下のようにファイル出力する。
- ファイル形式は、markdown形式で出力する。
- ファイル名は、"名前_yyyyMMdd.md"の形式をデフォルトとする。
- 保存先は、ファイル保存ダイアログで利用者が指定する。初期ファイル名にはデフォルトのファイル名を設定する。
- 名前にファイル名として使用できない文字が含まれる場合は、デフォルトのファイル名を生成する際に`_`へ置換する。
- 同名ファイルが存在する場合は、ファイル保存ダイアログの上書き確認を表示する。
- 保存をキャンセルした場合は、ファイルを出力せず、入力内容を維持する。
- 書き込み権限不足、ファイル使用中などにより保存に失敗した場合は、原因が分かるエラーメッセージを表示し、入力内容を維持する。
- 保存する内容は、以下の形式で出力する。
  - タイトルを「（名前）作業日報」と表示する：名前はカッコで囲む。
  - 日付を「yyyy.MM.dd」で表示する。
  - 作業内容として、「定例作業」と「スキルアップ活動」を表示する。
    - 定例作業は、入力済みの行を箇条書きで表示する。入力済みの行がない場合は、「定例作業なし」と表示する。
    - スキルアップ活動は、「スキルアップ活動」の項目に、リストを箇条書きで表示する。
    - スキルアップ活動に入力済みの行がない場合は、「スキルアップ活動なし」と表示する。
  - 成果物として、成果物名とリンクをセットで箇条書きで表示する。
  - 成果物に入力済みの行がない場合は、「成果物なし」と表示する。成果物名のみの場合は、成果物名を通常の箇条書きで表示する。
  - 名前、作業内容、成果物名に含まれるMarkdownの制御文字（`\\`、`*`、`_`、`#`、`[`、`]`）はエスケープして出力する。リンクはURL検証済みの値を使用する。
- 保存する内容の例は以下の通りである。
```markdown
## （名前）作業日報

yyyy.MM.dd

### 作業内容
- 定例作業リストの各項目
- スキルアップ活動
  - スキルアップ活動リストの各項目

### 成果物
- [成果物名](リンク)
```

### 3.7 テスト観点
以下の観点で、ユニットテストプロジェクトを作成する。
- 名前、日付、作業内容および成果物の必須・形式検証が仕様どおりに動作すること。
- 空行を除外し、定例作業・スキルアップ活動・成果物の「なし」表示が仕様どおりに出力されること。
- 成果物名のみ、成果物名と有効なURL、リンクのみ、無効なURLの各入力を検証すること。
- 日本語、Markdown制御文字、ファイル名に使用できない文字を含む入力で、Markdownとデフォルトファイル名が正しく生成されること。
- 保存成功、キャンセル、上書き確認、書き込み失敗時に、適切なメッセージと入力内容の維持が行われること。
